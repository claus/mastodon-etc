---
layout: '@/components/ui/MDXPostLayout/MDXPostLayout.astro'
title: Emojis Personalizados
publishedAt: '2022-11-26T12:00:00.000Z'
description: Emoji.
---

import EmojiPicker from '@/components/ui/EmojiPicker/EmojiPicker.astro';
import { fetchEmojis } from '@/utils/fetchEmojis';

export const prerender = true;

export const data = await fetchEmojis();

# Emojis Personalizados da {data.domain}

<EmojiPicker
    categories={data.categories}
    baseUrl={data.baseUrl}
    status={data.status}
    statusText={data.statusText}
/>
